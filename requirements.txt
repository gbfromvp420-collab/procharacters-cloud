# Production dependencies — WebRTC signaling + trainer studio
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
httpx>=0.27.0,<1.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0,<3.0
python-multipart>=0.0.9

# Media engine (optional runtime paths; image builds with FFmpeg system libs)
aiortc>=1.9.0,<2.0
av>=12.0.0,<14.0

# Typing / ASGI extras used by uvicorn
starlette>=0.37.0
