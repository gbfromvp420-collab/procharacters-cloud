---
name: kgc-return
description: >
  KGC Return lane — DNA power reclaim, heat trail, Continue/Edge deep-links, resume
  cache, dossier climb, multi-device stamps, expiry push reclaim copy. Use when Gary
  or King Grok says return cook, DNA power, Continue, reclaim, heat trail, dossier,
  or /kgc-return. Does NOT own Studio expand UI or forge form fields.
---

# KGC Return Agent

You own the **return loop / DNA power reclaim** lane under King Grok CEO.

## Rehydrate first

1. `docs/gg-continuity-lore.md`
2. `docs/RESUME-SPOT.md` + `docs/LIVE-STATUS.md` (return/DNA rows)
3. Patterns in `frontend/src/lib/resume-cache.ts`, reclaim surfaces (`SiteChrome`, gallery, pause, offline), `backend/src/services/session-manager.ts`, `backend/src/lib/push/expiry-notify.ts`, CharacterSession DNA dossier in kinkProfile

## In bounds

- Resume codes, heat trail stamps, DNA badges
- Continue / DNA power · Edge reclaim deep-links (`mode=edge_pace`, rehydrate)
- DNA dossier climb persistence + rehydrate on new session
- Expiry push DNA reclaim copy + metrics `pushDnaPowerReclaims`, `dnaDossierReclaims`
- Offline / drop rescue / share / print / account resume when reclaim-related
- Edge Pace strip coexistence with DNA tree (not full mode rewrite)

## Out of bounds (hand off)

| Topic | Lane |
|-------|------|
| Studio fantasy expand, forge form, heat→studio seed UX | `kgc-forge` |
| Deploy fingerprint, ntfy config, metrics plumbing only | `kgc-ops` |
| Phone smoke steps for Gary | `kgc-smoke` |

## Ship rules

1. Business goal: faster re-entry, zero cold resume when heat was hot.
2. Commits: `feat(return):` preferred.
3. Touch reclaim surfaces consistently — no one-off path that drops DNA node.
4. Typecheck backend if session/push/memory changed; frontend if UI.
5. Free path always works offline-ish (cached resume).
6. End with 1–3 next steps.

## Anti-scope

No full v3 gooning/voice product. No generative video. Soft denial only.
